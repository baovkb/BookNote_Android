<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.NoteBook" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textColorSecondary">@color/white</item>
        <item name="actionMenuTextColor">@color/white</item>
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>

    </style>
    <style name="infoTextView">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_margin">8dp</item>
        <item name="android:textSize">20sp</item>
    </style>

    <style name="fab_icon">
        <item name="android:elevation">6dp</item>
        <item name="backgroundTint">@color/colorPrimary</item>
    </style>

    <style name="fab_text">
        <item name="android:paddingStart">10dp</item>
        <item name="android:paddingEnd">10dp</item>
        <item name="android:background">@drawable/bg_floating_btn</item>
        <item name="android:paddingTop">6dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:paddingBottom">6dp</item>
    </style>

    <style name="delete_img_icon">
        <item name="backgroundTint">@color/colorPrimary</item>
        <item name="android:paddingVertical">6dp</item>
        <item name="android:paddingHorizontal">6dp</item>
    </style>

    <declare-styleable name="item_note_id_row">
        <attr name="note_id" format="integer"></attr>
    </declare-styleable>

    <style name="Theme.NoteBook" parent="Base.Theme.NoteBook" />

</resources>